spring:
  application:
    name: cloud-config
  security:
    basic:
      enabled: true
    user:
      name: cloud-config
      password: ${REGISTRY_SERVER_PASSWORD:cloud-config}
  profiles:
    active: dev
