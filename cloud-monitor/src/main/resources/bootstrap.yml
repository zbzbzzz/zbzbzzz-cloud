spring:
  application:
    name: cloud-monitor
  security:
    basic:
      enabled: false
    user:
      name: cloud-admin
      password: ${REGISTRY_SERVER_PASSWORD:cloud-admin}
  profiles:
    active: dev
